# Backend

This folder will contain the Node.js + Express backend API.

The backend will:
- Read data from PostgreSQL
- Expose REST API endpoints
- Serve data to the React frontend

Implementation will be completed in later phases.